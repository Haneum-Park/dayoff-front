'use client';

import styled from '@emotion/styled';
import { Flex } from '@radix-ui/themes';

export const CommonSwitchWrap = styled(Flex)`
  border: 1px solid var(--gray-12);
  background-color: var(--gray-1);
`;